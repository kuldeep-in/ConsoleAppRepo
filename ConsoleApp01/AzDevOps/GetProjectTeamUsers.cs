using System;
using System.Collections.Generic;
using System.Text;
//Using Microsoft.TeamFoundation.Core.WebApi,

namespace ConsoleApp01.AzDevOps
{
    class GetProjectTeamUsers
    {
        public static void GetProjectTeamUser()
        {
            //using (TeamHttpClient teamHttpClient = new TeamHttpClient(uri, credentials))
            //{
            //    var teams = await teamHttpClient.GetTeamsAsync(azureDevOpsprojectId);
            //    return teams;
            //};
            //and then get each member as:
            //using (TeamHttpClient teamHttpClient = new TeamHttpClient(uri, credentials))
            //{
            //    var teamMembers = await teamHttpClient.GetTeamMembersWithExtendedPropertiesAsync(azureDevOpsprojectId, teamId);
            //    return teamMembers;
            //};
        }
    }
}
